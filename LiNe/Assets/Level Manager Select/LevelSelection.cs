using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using TMPro;
/* SUBSCRIBING TO MY YOUTUBE CHANNEL: 'VIN CODES' WILL HELP WITH MORE VIDEOS AND CODE SHARING IN THE FUTURE :) THANK YOU */

public class LevelSelection : MonoBehaviour
{
    //public Button[] lvlButtons;
    //public MoveToNextLevel modebtn;
    //int levelAt;
    //public TMP_Text bestscore;
    //public string key;
    //// Start is called before the first frame update
    //void Start()
    //{
    //        levelAt = PlayerPrefs.GetInt(modebtn.unlockedbtn, 0); /* < Change this int value to whatever your
    //                                                         level selection build index is on your
    //                                                         build settings */
    //    for (int i = 0; i < lvlButtons.Length; i++)
    //    {
    //        lvlButtons[i].transform.GetChild(0).GetComponent<Text>().text = (i + 1).ToString();
    //        if (i  > levelAt)
    //            lvlButtons[i].interactable = false;
    //        lvlButtons[i].transform.name = modebtn.lvlofmode + i;
    //        lvlButtons[i].transform.GetChild(0).transform.name = i.ToString();
    //    }
    //    bestscore.text = PlayerPrefs.GetFloat(key).ToString("0");

    //}

}
